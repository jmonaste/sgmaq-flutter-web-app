name: flutter_application_sgmaq
description: "A new Flutter project."
publish_to: 'none' # Remove this line if you wish to publish to pub.dev

version: 0.0.1+1

environment:
  sdk: ^3.4.4

dependencies:
  flutter:
    sdk: flutter
  http: ^0.13.3
  dio: ^5.2.0
  flutter_secure_storage: ^8.0.0
  image_picker: ^0.8.4+4
  english_words: ^4.0.0
  provider: ^6.0.0
  camera: ^0.10.0
  file_picker: ^5.2.4
  qr_code_scanner: ^1.0.1
  shared_preferences: ^2.0.9
  flutter_speed_dial: ^7.0.0
  permission_handler: ^10.0.0
  google_fonts: ^2.1.0
  fl_chart: ^0.69.0
  intl: ^0.17.0

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^2.0.0

flutter:
  uses-material-design: true
  assets:
  - assets/